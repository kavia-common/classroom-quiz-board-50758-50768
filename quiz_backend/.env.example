# Backend environment example for Django + PostgreSQL
# These are read by config/settings.py

# Database connection (aligns with quiz_database container, port 5001)
POSTGRES_DB=quizdb
POSTGRES_USER=quizuser
POSTGRES_PASSWORD=quizpass
POSTGRES_HOST=quiz_database
POSTGRES_PORT=5001

# Optional tuning
DB_CONN_MAX_AGE=60

# CORS frontend allowance
FRONTEND_URL=http://localhost:3000
CORS_ALLOW_ALL=true
